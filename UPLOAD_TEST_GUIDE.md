# 🎯 دليل سريع لاختبار رفع الصور

## 🔗 الروابط المباشرة

### 📸 صفحة رفع الصور (مخصصة)
**الرابط:** https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/static/upload-test.html

**المميزات:**
- ✅ سحب وإفلات الصور
- ✅ معاينة فورية
- ✅ شريط تقدم الرفع
- ✅ مكتبة صور تفاعلية
- ✅ نسخ روابط الصور
- ✅ حذف الصور

---

### ✍️ صفحة إضافة مقال (بسيطة)
**الرابط:** https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/test-admin

**المميزات:**
- ✅ إضافة مقال بسرعة (عربي/إنجليزي)
- ✅ عرض جميع المقالات
- ✅ حذف المقالات
- ⏳ قريباً: رفع الصورة الرئيسية مع المقال

---

### 🎛️ لوحة التحكم الكاملة
**الرابط:** https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/admin

**بيانات الدخول:**
- 👤 المستخدم: `admin`
- 🔑 كلمة المرور: `admin123`

---

## 🧪 كيفية الاختبار

### 1️⃣ اختبار رفع صورة واحدة

```bash
# خطوة 1: افتح صفحة رفع الصور
https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/static/upload-test.html

# خطوة 2: اسحب صورة من جهازك أو اضغط للاختيار

# خطوة 3: أضف وصف للصورة (اختياري):
# - وصف عربي: "صورة عملية جراحية"
# - وصف إنجليزي: "Surgical procedure"

# خطوة 4: اضغط "رفع الصورة"

# خطوة 5: شاهد الصورة تظهر في مكتبة الصور 🎉
```

---

### 2️⃣ اختبار رفع صور متعددة

```javascript
// يمكنك رفع صور متعددة واحدة تلو الأخرى
// كل صورة ستحصل على:
// - معرف فريد (ID)
// - اسم ملف فريد (timestamp-random.jpg)
// - رابط مباشر (/static/uploads/...)
// - معاينة فورية
```

---

### 3️⃣ اختبار الـ API مباشرة

```bash
# اختبار رفع صورة عبر curl
curl -X POST http://localhost:3000/api/admin/media/upload-file \
  -F "file=@/path/to/image.jpg" \
  -F "alt_text_ar=وصف الصورة" \
  -F "alt_text_en=Image description"

# عرض جميع الصور
curl http://localhost:3000/api/admin/media?type=image | jq

# حذف صورة
curl -X DELETE http://localhost:3000/api/admin/media/1
```

---

## 📊 أمثلة عملية

### مثال 1: رفع صورة رئيسية لمقال

1. افتح: `/static/upload-test.html`
2. ارفع صورة (مثال: `surgeon.jpg`)
3. انسخ رابط الصورة (مثال: `/static/uploads/1772229123456-abc123.jpg`)
4. افتح: `/test-admin`
5. أضف مقال واستخدم الرابط في حقل "رابط الصورة الرئيسية"

---

### مثال 2: إنشاء معرض صور

```javascript
// رفع 5 صور
const images = [
  'before-surgery.jpg',
  'during-surgery.jpg',
  'after-surgery.jpg',
  'patient-recovery.jpg',
  'final-result.jpg'
];

for (const img of images) {
  const formData = new FormData();
  formData.append('file', img);
  await axios.post('/api/admin/media/upload-file', formData);
}

// الآن يمكنك استخدام روابطها في محتوى المقال
```

---

## ✅ التحقق من النجاح

### ✔️ صورة مرفوعة بنجاح إذا:
1. ظهرت رسالة "تم رفع الصورة بنجاح! 🎉"
2. ظهرت الصورة في مكتبة الصور
3. يمكنك نسخ رابط الصورة
4. يمكنك حذف الصورة

### ❌ فشل الرفع إذا:
- نوع الملف غير مدعوم (يجب أن يكون: JPEG, PNG, WebP, GIF)
- حجم الملف أكبر من 5MB
- لا توجد صورة محددة

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Invalid file type"
**الحل:** استخدم صور بصيغة JPEG, PNG, WebP, أو GIF فقط

### المشكلة: "File too large"
**الحل:** قلل حجم الصورة لأقل من 5MB

### المشكلة: الصورة لا تظهر في المكتبة
**الحل:** اضغط زر "تحديث" أو أعد تحميل الصفحة

### المشكلة: لا يمكن رفع الصورة
**الحل:** 
1. تأكد من اتصال الإنترنت
2. افتح Console في المتصفح (F12)
3. ابحث عن رسائل الخطأ
4. تحقق من أن الخادم يعمل

---

## 📱 التوافق

- ✅ Chrome / Edge (مُوصى به)
- ✅ Firefox
- ✅ Safari
- ✅ Mobile browsers

---

## 🚀 الخطوات التالية

بعد نجاح الاختبار، يمكنك:
1. ✅ دمج رفع الصور مع نموذج إضافة المقالات
2. ✅ إضافة محرر نصوص متقدم (Quill.js)
3. ✅ إضافة معالج صور (قص، تدوير، فلاتر)
4. ✅ تفعيل Cloudflare R2 للإنتاج

---

**تاريخ:** 2026-02-27  
**الحالة:** ✅ جاهز للاختبار الفوري

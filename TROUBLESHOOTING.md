# 🔍 دليل استكشاف الأخطاء - لوحة التحكم

## ✅ الخطوات للتحقق من عمل النظام:

### 1. الوصول للوحة التحكم:

**الرابط الصحيح:**
```
https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/static/admin
```

(بدون .html في النهاية)

### 2. تسجيل الدخول:
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### 3. اختبار إضافة مقال:

**الطريقة الأولى - عبر الواجهة:**
1. افتح الرابط أعلاه
2. سجل دخول
3. اضغط زر "مقال جديد" (الزر الأزرق أعلى اليمين)
4. املأ البيانات المطلوبة:
   - عنوان المقال (عربي) *إلزامي*
   - Article Title (English) *إلزامي*
   - المحتوى (عربي) *إلزامي*
   - Content (English) *إلزامي*
5. اختر التصنيف
6. اختر حالة النشر (منشور/مسودة)
7. اضغط "حفظ المقال"

**الطريقة الثانية - عبر API مباشرة:**

```bash
# 1. تسجيل الدخول أولاً
curl -X POST https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/api/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 2. إضافة مقال
curl -X POST https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/api/admin/articles \
  -H "Content-Type: application/json" \
  -d '{
    "title_ar": "مقال تجريبي",
    "title_en": "Test Article",
    "content_ar": "محتوى تجريبي",
    "content_en": "Test content",
    "category": "health",
    "is_published": 1
  }'

# 3. عرض المقالات
curl https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/api/admin/articles
```

---

## 🐛 إذا ظهرت أخطاء:

### مشكلة: زر "مقال جديد" لا يعمل

**الحل:**
1. افتح Console المتصفح (F12)
2. تحقق من وجود أخطاء JavaScript
3. تأكد من تحميل ملف `/static/admin-advanced.js`

**اختبار يدوي:**
```javascript
// في Console المتصفح، جرب:
showArticleForm()
// يجب أن يظهر النموذج
```

### مشكلة: النموذج لا يُحفظ

**السبب المحتمل:**
- حقول إلزامية فارغة
- مشكلة في الاتصال بالـ API

**الحل:**
1. تحقق من ملء جميع الحقول الإلزامية (*)
2. افتح Network Tab في Console
3. انظر لطلب POST إلى `/api/admin/articles`
4. تحقق من Response

### مشكلة: 401 Unauthorized

**السبب:**
لم تسجل الدخول

**الحل:**
1. افتح الصفحة من جديد
2. سجل دخول بـ admin / admin123

---

## ✅ اختبار النظام الكامل:

### Script اختبار شامل:

```bash
#!/bin/bash

echo "🔍 اختبار نظام CMS..."

# 1. Health Check
echo "\n1️⃣ Health Check:"
curl -s http://localhost:3000/api/health | jq .

# 2. Login
echo "\n2️⃣ تسجيل الدخول:"
LOGIN=$(curl -s -X POST http://localhost:3000/api/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}')
echo $LOGIN | jq .

# 3. إضافة مقال
echo "\n3️⃣ إضافة مقال:"
curl -s -X POST http://localhost:3000/api/admin/articles \
  -H "Content-Type: application/json" \
  -d '{
    "title_ar": "مقال اختبار آلي",
    "title_en": "Auto Test Article",
    "content_ar": "محتوى",
    "content_en": "Content",
    "is_published": 1
  }' | jq .

# 4. عرض المقالات
echo "\n4️⃣ المقالات الموجودة:"
curl -s http://localhost:3000/api/admin/articles | jq '.articles | length'

# 5. رفع صورة
echo "\n5️⃣ رفع صورة:"
curl -s -X POST http://localhost:3000/api/admin/media/upload \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "test.jpg",
    "file_url": "https://images.unsplash.com/photo-1579684385127-1ef15d508118?w=400",
    "file_type": "image",
    "mime_type": "image/jpeg"
  }' | jq .

# 6. عرض الوسائط
echo "\n6️⃣ الوسائط الموجودة:"
curl -s http://localhost:3000/api/admin/media | jq '.media | length'

echo "\n✅ انتهى الاختبار!"
```

---

## 📋 Checklist:

قبل الإبلاغ عن مشكلة، تأكد من:

- [ ] تم فتح الرابط الصحيح `/static/admin`
- [ ] تم تسجيل الدخول بنجاح
- [ ] تم ملء جميع الحقول الإلزامية (*)
- [ ] Console المتصفح لا يحتوي على أخطاء حمراء
- [ ] Network Tab يظهر request ناجح (200 OK)

---

## 🆘 إذا استمرت المشكلة:

1. افتح Console المتصفح (F12)
2. انسخ جميع الأخطاء الموجودة
3. افتح Network Tab
4. اضغط "مقال جديد" واضغط "حفظ"
5. انسخ Request & Response من Network Tab
6. شارك هذه المعلومات

---

## 🎯 الروابط المهمة:

- **لوحة التحكم**: https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/static/admin
- **API Docs**: انظر `ADMIN_ACCESS_FIX.md`
- **دليل المستخدم**: انظر `USER_GUIDE_QUICK.md`

---

## ✅ النظام يعمل إذا:

✅ تم إضافة 3 مقالات بنجاح (تحقق عبر API)  
✅ API يستجيب لجميع الطلبات  
✅ قاعدة البيانات محدّثة  
✅ جميع الملفات موجودة  

**المشكلة المحتملة: واجهة المستخدم فقط!**

**الحل السريع**: استخدم API مباشرة حتى يتم إصلاح الواجهة.

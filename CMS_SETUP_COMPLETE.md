# 🎉 نظام إدارة المحتوى (CMS) - نسخة بسيطة

## ✅ تم إنشاء النظام بنجاح!

### 📊 المميزات المتوفرة:

#### 1. إدارة المقالات 📝
- ✅ إضافة مقالات جديدة
- ✅ تعديل المقالات الموجودة
- ✅ حذف المقالات
- ✅ نشر/إخفاء المقالات
- ✅ دعم اللغتين العربية والإنجليزية
- ✅ إضافة صور رئيسية للمقالات
- ✅ تصنيف المقالات (صحة، جراحة، وقاية، تغذية)
- ✅ متابعة عدد المشاهدات

#### 2. إعدادات الموقع ⚙️
- ✅ التحكم في شعار الموقع (Logo)
- ✅ اختيار اللغة الأساسية (العربية/الإنجليزية)
- ✅ اختيار اللغة الثانوية
- ✅ تفعيل/تعطيل اللغة الثانوية

### 🔐 بيانات الدخول:
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

⚠️ **مهم جداً:** يرجى تغيير كلمة المرور بعد أول تسجيل دخول!

---

## 🌐 الروابط المهمة:

### 🖥️ لوحة التحكم:
- **صفحة الدخول:** https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/admin
- **لوحة التحكم الكاملة:** https://3000-i1lbhn0vco2lmzbjr48gv-5185f4aa.sandbox.novita.ai/static/admin.html

### 🔌 API Endpoints:

#### المصادقة (Authentication):
```bash
POST /api/admin/auth/login
Body: { "username": "admin", "password": "admin123" }
```

#### المقالات (Articles):
```bash
GET    /api/admin/articles          # جلب جميع المقالات
GET    /api/admin/articles/:id      # جلب مقال محدد
POST   /api/admin/articles          # إضافة مقال جديد
PUT    /api/admin/articles/:id      # تعديل مقال
DELETE /api/admin/articles/:id      # حذف مقال
```

#### الإعدادات (Settings):
```bash
GET  /api/admin/settings             # جلب جميع الإعدادات
GET  /api/admin/settings/:key        # جلب إعداد محدد
PUT  /api/admin/settings/:key        # تحديث إعداد
POST /api/admin/settings/bulk        # تحديث مجموعة إعدادات
```

---

## 📦 قاعدة البيانات:

### الجداول (Tables):
1. **users** - مستخدمي النظام
2. **articles** - المقالات (عربي/إنجليزي)
3. **article_images** - صور المقالات
4. **site_settings** - إعدادات الموقع

### موقع قاعدة البيانات:
```
.wrangler/state/v3/d1/miniflare-D1DatabaseObject/doctor-db-production.sqlite
```

---

## 🛠️ أوامر مفيدة:

### إدارة قاعدة البيانات:
```bash
# جلب جميع المقالات
npx wrangler d1 execute doctor-db-production --local --command="SELECT * FROM articles"

# جلب الإعدادات
npx wrangler d1 execute doctor-db-production --local --command="SELECT * FROM site_settings"

# حذف جميع المقالات
npx wrangler d1 execute doctor-db-production --local --command="DELETE FROM articles"

# إعادة تعيين قاعدة البيانات
rm -rf .wrangler/state/v3/d1
npx wrangler d1 migrations apply doctor-db-production --local
```

### إدارة الخادم:
```bash
# بناء المشروع
npm run build

# تشغيل الخادم
pm2 start ecosystem.config.cjs

# إعادة تشغيل
pm2 restart doctor-surgeon-website

# الحالة
pm2 status

# السجلات
pm2 logs doctor-surgeon-website --nostream
```

---

## 📝 مثال على إضافة مقال عبر API:

```bash
curl -X POST http://localhost:3000/api/admin/articles \
  -H "Content-Type: application/json" \
  -d '{
    "title_ar": "الوقاية من سرطان القولون",
    "title_en": "Colon Cancer Prevention",
    "excerpt_ar": "نصائح مهمة للوقاية من سرطان القولون والكشف المبكر",
    "excerpt_en": "Important tips for colon cancer prevention and early detection",
    "content_ar": "محتوى المقال بالعربية...",
    "content_en": "Article content in English...",
    "main_image_url": "https://example.com/image.jpg",
    "category": "prevention",
    "is_published": 1
  }'
```

---

## 🚀 الخطوات القادمة المقترحة:

### 1. تحسينات الأمان 🔒
- [ ] تطبيق JWT للمصادقة
- [ ] تشفير كلمات المرور باستخدام bcrypt
- [ ] إضافة حماية CSRF
- [ ] تحديد صلاحيات المستخدمين

### 2. تحسينات المقالات 📰
- [ ] إضافة محرر نصوص غني (Rich Text Editor)
- [ ] رفع الصور مباشرة إلى R2
- [ ] إضافة الوسوم (Tags)
- [ ] جدولة النشر التلقائي
- [ ] معاينة المقال قبل النشر

### 3. تحسينات الإعدادات ⚙️
- [ ] إدارة القوائم (Menu Management)
- [ ] إعدادات SEO
- [ ] إدارة وسائل التواصل الاجتماعي
- [ ] إعدادات الألوان والتصميم

### 4. لوحة المعلومات 📊
- [ ] إحصائيات المقالات
- [ ] تحليلات الزيارات
- [ ] آخر التعديلات
- [ ] نشاط المستخدمين

### 5. الربط مع الواجهة الأمامية 🌐
- [ ] جلب المقالات من قاعدة البيانات
- [ ] عرض اللوجو من الإعدادات
- [ ] تطبيق إعدادات اللغة
- [ ] تحديث عدد المشاهدات

---

## 📊 الحالة الحالية:

### ✅ منجز (100%):
- [x] قاعدة البيانات D1
- [x] API إدارة المقالات
- [x] API إعدادات الموقع
- [x] لوحة تحكم إدارية
- [x] نظام مصادقة بسيط

### ⏳ المرحلة القادمة:
1. تحسين الأمان (JWT + bcrypt)
2. ربط الواجهة الأمامية بقاعدة البيانات
3. إضافة محرر نصوص غني
4. رفع الصور إلى R2

---

## 📌 ملاحظات:

1. **الأمان**: النظام الحالي يستخدم مصادقة بسيطة. يُنصح بتطبيق JWT و bcrypt في الإنتاج.

2. **الصور**: حالياً يتم إدخال روابط الصور يدوياً. يُفضل إضافة نظام رفع صور إلى Cloudflare R2.

3. **المحرر**: استخدام textarea بسيط. يُنصح بإضافة محرر نصوص غني مثل TinyMCE أو Quill.

4. **الواجهة الأمامية**: حالياً الموقع لا يزال يستخدم بيانات ثابتة. يجب ربطه بقاعدة البيانات.

---

## 🎯 ملخص سريع:

✅ **تم إنشاء:**
- قاعدة بيانات D1 كاملة
- API للمقالات والإعدادات
- لوحة تحكم إدارية
- نظام مصادقة

🔗 **الوصول:**
- لوحة التحكم: `/admin` أو `/static/admin.html`
- API: `/api/admin/*`

🔐 **الدخول:**
- المستخدم: `admin`
- كلمة المرور: `admin123`

---

## 📞 الدعم:

إذا كان لديك أي استفسارات أو تحتاج إلى مساعدة، يرجى التواصل معي!

**تاريخ الإنشاء:** 27 فبراير 2026
**الإصدار:** 1.0.0
